# 重构（RESTYLE HIGH-LEVEL INTERFACE）

**定位**：重构体系的高层接口（对外唯一门面）。独立入口，不经 全局调度器。
**职责**：接收用户的任意重构请求（文件／粘贴文本／范围／联动／设计／格式变／零输入），做输入分流与输出分流，执行转发到 基础模块 `视频模块/重构基础模块.md`。

## 强制阅读流程（★硬性，按序读完才能进入下一步）

**阶段一｜启动必读（无条件，逐文件按序读，读完全部才允许发起任何交互选项或执行）**

| 序 | 文件 | 目的 |
|---|---|---|
| 1 | 根目录 `跟新规范.md` | 强制重新记忆 `规范/项目规范.txt`（规范文件夹/文件缺失 → 按其规则直接终止） |
| 2 | `规范/异常设计.md` | 异常记录格式与处理原则（本文所有异常出口均引用它） |
| 3 | `视频模块/重构基础模块.md` | 执行协议：两种模式／格式轴／设计重构／内容重构／交互规则／新风格来源 |
| 4 | 通用模块四库，固定顺序：`色彩模块.txt` → `光线模块.txt` → `材质模块.txt` → `皮肤模块.txt` | 参数知识库（顺序与 图片/视频模块 一致） |
| 5 | `通用模块/联动模块.md` | 联动规则 R1-R5／写实光线硬性要求／扩写成句协议／参数设计表／自检清单 |

**阶段二｜条件必读（进入分流后触发；触发即强制，禁止跳过）**

| 条件 | 必读文件 |
|---|---|
| 涉及参考图（上传图片/资产路径） | `通用模块/图片分析限制模块.md` |
| 目标为基础格式 或 指令含「格式变」 | `视频模块/基础格式.txt` |

**目标提示词文件**：输入分流定位后立即读取（未定位到 → 按【硬性边界 5】处理）。

**阅读纪律**：
- 逐文件完整阅读，禁止跳读、禁止只读标题、禁止凭上次会话记忆代替阅读
- 阶段一任一文件缺失 → 按 `规范/异常设计.md` 处理 → 终止；阶段二触发的文件缺失 → 同样处理
- 未完成阶段一 → 发起的选项、执行的转发一律无效

## 触发方式（四选一）

0. **零输入**：什么都不带 → 先向用户发起 **来源询问**（选项 + 自定义输入，二选一）：
   - 提供 **提示词文件路径** ＋ **图片资产路径**（联动/设计重构需要参考资产图时必须）
   - 或 直接 **粘贴提示词** ／ **上传图片**
   来源明确后 → 再发起 **全选项表**（见 基础模块【交互规则】：重构类型 / 范围 / 风格，逐项点选）
1. 指定提示词文件（路径或文件名）：

   ```text
   重构 → <提示词文件> 重构 图片N / 视频N / 所有图片 / 所有视频 [新风格]
   ```

2. 单个条目（上下文中已有目标提示词文件时）：

   ```text
   重构 → 图片N / 视频N [新风格]
   ```

3. 直接粘贴提示词文本 或 上传图片（不是文件路径）+ 新风格（可选）

指令含「联动」→ 联动重构；含「设计重构」→ 视听语言重设计（先发起选项）；含「内容重构」→ 内容层修改（先发起选项）；含「格式变」→ 格式转换；否则 → 普通重构。模式与护栏由基础模块统一裁决。
零输入 → 按【触发方式 0】先问来源（文件路径＋资产路径 或 粘贴/上传），拿不到就反复询问，**禁止无目标执行**

## 输入分流

- 指定提示词文件 → 读取该文件，定位条目，进入转发流程
- 粘贴文本 → 以粘贴内容为目标条目，重构后走对话框输出
- 上传图片 → 先判定图片类型，再分流：
  - **站位图** → 仅提取站位（配合 联动／设计重构），不提材质与风格
  - **非站位图** → 同时满足以下两个条件，才**触发 `视频模块/调度器.md` 步骤3 图片模式**（完整执行该链路：图片分析限制模块 → 色彩/光线/材质/皮肤 四库 → 联动模块 → 参数设计表 → 自检清单）：
    1. 用户**明确要求更改画面风格**
    2. 当前条目的参数**不满足**该画面风格
    - 条件缺任何一个（没说改风格，或当前参数已满足）→ **不触发**该链路，直接按现有流程重构
    - 图片分析结果只取**材质参数**；画面风格仍由用户文字要求决定，禁止从图片提取风格
    - **要求格式不变** → 图片模式链路的最后一步（基础格式输出）**跳过**，按原格式或用户指定格式输出
- 无法判定目标 → 发起目标选项，不猜测

## 输出分流（限制）

- **直接粘贴提示词 / 上传图片**（非文件路径）→ 重构结果直接输出在对话的**同一个文本框内**（单个代码块），可直接复制使用
  - ★ **必须输出完整提示词**：全部槽位逐槽写满（资产参考→场景→空间布局→人物→道具→拍摄参数→影调→人物情绪时间轴→镜头N→逻辑约束→负面提示词→声音时间轴），禁止只输出改动片段／摘要／省略号代替
  - 未改动的槽位也必须原样输出，保证拿到手即可直接使用
  - 一行可以容纳多个字，但不要把所有内容挤在一行 —— 按提示词结构合理换行
- **指定提示词文件** → 不输出提示词，直接修改该文件（原地替换对应条目）——★ **唯一豁免**：目标是修改文件时，无需在对话框输出完整提示词，只报告改动清单

## 转发流程

1. 目标：解析范围（图片N／视频N／所有图片／所有视频）、模式（普通／联动／设计／内容）、格式轴（是否含「格式变」）
2. 格式规则（按来源判定）：
   - 目标位于 **文本拆分输出的单集文件夹**（数字命名文件夹内的提示词文件）→ 提示词格式为 `视频模块/基础格式.txt`，按基础格式重构
   - **其他来源**（任意路径 或 用户上传）→ **提示词格式不变**，按其自身结构重构
   - 指令含「格式变」→ 按基础模块【格式轴】执行格式转换（默认转 基础格式.txt，或用户指明的格式）
3. 执行：转发到 `视频模块/重构基础模块.md`，遵守其【两种模式】【格式轴】【设计重构】【内容重构】【交互规则】【新风格来源】
   - 新风格参数优先取自 通用模块 四库 + 联动模块（参数设计表 → 自检清单）
4. 完成：按输出分流交付（原地替换 或 对话框输出），报告改动清单

## 硬性边界

1. **格式默认不变**：目标是什么格式，输出还是什么格式；指令含「格式变」→ 按基础模块【格式轴】转换
2. ★ 原文忠实硬性限制：剧本/提示词原有内容（台词、剧情事实、人物、道具、逻辑约束）**只读**——禁止改写、禁止遗漏、禁止概括缩写；**原有设计（场景布置／人物外形服装／道具外观设定）不得忽略**——重构后这些设定必须保留并体现在对应槽位，禁止替换成自建设计；不增删条目，不移动内容位置，条目数量与顺序始终禁止修改；**唯一放行入口 = 用户明确的修改要求**，且内容层改动必须走 基础模块【内容重构】并注明改动理由
3. 冲突或无法判定 → 按 `规范/异常设计.md` 写入；问题过于严重 → 询问用户
4. ★ 架构文件缺失处理：依赖的架构文件（`视频模块/重构基础模块.md`、`通用模块/图片分析限制模块.md`、`通用模块/联动模块.md`、通用模块四库）预设路径不存在 → **先尝试寻找**（工作区范围内搜索同名文件）→ 确实找不到 → 按 `规范/异常设计.md` 写入 `异常/日期异常.md` → 直接终止运行（不编造路径、不用默认顶替、不跳过继续）
5. 指定的提示词文件/资产路径：先尝试寻找（按路径 → 工作区范围内搜索同名文件）→ 确实找不到且用户无法提供 → 异常 → 直接终止（不在无目标状态下继续执行）
