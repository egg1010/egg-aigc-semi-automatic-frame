# 跟新规范（MANDATORY SPEC RE-MEMORIZATION）

**定位**：规范强制记忆入口（高层接口，只负责规范记忆，不负责底层模块事务）。所有调度器（全局/文本/图片提示词/视频）与 设计与重构 启动第一步必须运行本文件。
**作用**：强制 AI 重新记忆项目规范——禁止凭印象跳过、禁止沿用上一次会话的记忆结果。

## 强制记忆流程

1. 检查 规范文件夹 `规范/`：
   - 不存在 → 工作区范围内搜索同名文件夹 → 确实没有 → **直接终止运行**，AI 直接说「规范文件夹缺失」（不写任何异常文件）
2. 检查 规范文件 `规范/项目规范.txt`：
   - 没有 → 工作区范围内搜索同名文件 → 确实没有 → **直接终止运行**，AI 直接说「规范文件缺失」（不写任何异常文件）
   - 找到 → 进入第 3 步
3. 逐条阅读 → 强制重新记忆 `规范/项目规范.txt`：
   - 逐条记忆，并输出「记忆确认清单」：每条规范 → 本次执行如何遵守（一句话）；仅在对话输出，不写文件
   - 规范与后续模块流程冲突 → 以 项目规范 为准；无法落实的条目 → 按 `规范/异常设计.md` 写入 `规范/异常.md`
4. 文件存在但为空 → 输出「规范为空 → 默认执行」→ 各模块按各自默认参数执行（文件不存在 ≠ 文件为空，不混淆）
5. **记忆有效期：仅本次运行有效** —— 每次启动都必须重新运行本文件、重新记忆；禁止引用上一次的记忆结果直接开工

## 附带记忆（存在即记忆，不强制）

- `规范/多集要求.txt` 存在 → 一并记忆（视频模块多集执行要求）
- `规范/自定义拆分格式规范.txt` 存在 → 一并记忆（文本拆分格式）；缺失或为空 → 各自模块按默认执行
