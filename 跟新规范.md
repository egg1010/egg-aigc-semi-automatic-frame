# 跟新规范（MANDATORY READING REFRESH）

**定位**：规范与格式强制阅读入口（高层接口，只负责规范/格式阅读，不负责底层模块事务）。所有调度器（全局/文本/图片提示词/视频）与 设计与重构 启动第一步必须运行本文件。
**作用**：每次运行都重新阅读全部规范与格式文件——禁止凭印象跳过、禁止沿用上一次的阅读结果。

## 强制阅读流程（★每次运行都完整执行，无首次/后续之分）

1. 检查 规范文件夹 `规范/`：
   - 不存在 → 工作区范围内搜索同名文件夹 → 确实没有 → **直接终止运行**，AI 直接说「规范文件夹缺失」（不写任何异常文件）
2. 检查 规范文件 `规范/项目规范.txt`：
   - 没有 → 工作区范围内搜索同名文件 → 确实没有 → **直接终止运行**，AI 直接说「规范文件缺失」（不写任何异常文件）
   - 找到 → 进入第 3 步
3. 逐条阅读 `规范/项目规范.txt`：
   - 逐条阅读，并输出「阅读确认清单」：每条规范 → 本次执行如何遵守（一句话）；仅在对话输出，不写文件
   - 规范与后续模块流程冲突 → 以 项目规范 为准；无法落实的条目 → 按 `规范/异常设计.md` 写入 `异常/日期异常.md`
4. 文件存在但为空 → 输出「规范为空 → 默认执行」→ 各模块按各自默认参数执行（文件不存在 ≠ 文件为空，不混淆）
5. ★ **阅读方式硬性规定：不创建记忆文件，Agent 自己阅读并持有** —— 全流程禁止创建任何 记忆文件（记忆.md／笔记／摘要／缓存／memory 之类）；规范、格式、图片分析报告、参数设计表、清点名单等一律在 Agent 上下文（对话内）持有与传递，不落盘；违反 = 按 `规范/异常设计.md` 处理

## 通用模块字典强制查阅（★每次运行都执行）

参数设计前必须按序完整查阅以下字典，选参数以**本次查阅到的内容**为准：

| 序 | 字典 | 用途 |
|---|---|---|
| 1 | `通用模块/色彩模块.txt` | 色彩/调色参数 |
| 2 | `通用模块/光线模块.txt` | 布光参数（写实光线物理链依据） |
| 3 | `通用模块/材质模块.txt` | 材质参数 |
| 4 | `通用模块/皮肤模块.txt` | 皮肤参数 |
| 5 | `通用模块/联动模块.md` | 联动规则 R1-R5／写实光线硬性要求／扩写成句协议／参数设计表／自检清单 |

- 禁止凭上一次查阅的内容直接选参——字典可能已更新，必须重读
- 查阅后走 联动模块【深挖选参协议】→【参数设计表】→【自检清单】
- 字典文件缺失 → 先尝试寻找（工作区搜同名文件）→ 找不到 → 按 `规范/异常设计.md` 写入 → 终止

## 格式文件强制阅读（★每次运行都执行，逐文件按序完整阅读）

每次运行把以下格式文件重新阅读为最新内容，禁止凭上一次阅读输出任何提示词：

| 序 | 文件 | 用途 |
|---|---|---|
| 1 | `规范/异常设计.md` | 异常记录格式（写文件边界／异常.md 格式／处理原则） |
| 2 | `视频模块/基础格式.txt` | 视频提示词输出格式（资产参考置顶节／影调槽位／写实光线硬性） |
| 3 | `图片提示词模块/人物图片资产格式.txt` | 人物资产图提示词格式（头肩四宫格＋服装三视图＋负面提示词） |
| 4 | `规范/自定义拆分格式规范.txt` | 文本拆分格式（存在即读；缺失或为空 → 默认规则，不终止） |

**阅读纪律**：

- 逐文件完整阅读，禁止跳读、禁止只读标题、禁止以上一次阅读代替
- 序 1-3：预设路径不存在 → 先尝试寻找（工作区搜索同名文件）→ 确实找不到 → 按 `规范/异常设计.md` 写入 `异常/日期异常.md` → 终止运行
- 例外：`规范/异常设计.md` 本身缺失 → 无处记录异常 → AI 直接说「异常设计文件缺失」→ 终止（同 规范缺失 的启动例外逻辑）
- 序 4：缺失或为空 → 默认执行（不终止）

## 附带阅读（每次运行，存在即读，不强制）

- `规范/多集要求.txt` 存在 → 一并阅读（视频模块多集执行要求）
