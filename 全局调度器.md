# 全局调度器（GLOBAL DISPATCHER）

## 最高优先级规则

1. 禁止修改 模块文件（各模块文件夹下的文件均为只读）
2. 如果问题过于严重 → 询问用户
3. ★ 架构文件缺失处理：运行各模块调度器前，先确认【文件路径总表】中将被访问的 模块/格式 文件存在；预设路径不存在 → **先尝试寻找**（工作区范围内搜索同名文件）→ 确实找不到 → 按 `规范/异常设计.md` 写入 `规范/异常.md` → 直接终止运行（不编造路径、不用默认顶替、不跳过继续）
4. ★ 启动必读：本调度器与 各模块调度器、设计与重构 启动第一步 → 运行 根目录 `跟新规范.md`（强制重新记忆规范；寻找与异常规则见该文件）
5. ★ 流程零落盘：正常运行禁止写文件（业务交付物除外：拆分文件/资产表/提示词文件/输出表格）；中间产物只在对话输出；写文件的唯一异常场景 = 按 `规范/异常设计.md` 写 `规范/异常.md`

## 文件路径总表

- 规范/
- 文本模块/
- 图片提示词模块/
- 视频模块/
- 通用模块/
- 工具/

## 交互模式（选项式）

- **零输入**（用户只说"运行"或什么都没说）→ 发起 **全选项表**（逐项点选）：

1. 执行范围：全流程（文本→图片→视频）/ 仅文本模块 / 仅图片提示词模块 / 仅视频模块 / 自定义输入
2. 风格：默认「写实 实拍风格 电影感」/ 自定义输入
3. 图片提示词输出格式：文本文件（默认 .txt）/ Excel / Markdown / Word

- **每个选项必须附带「自定义输入」**：点选 + 补充输入 → 合并执行；纯文本输入 → 直接执行
- **指令完整**（范围/风格/输出格式明确）→ 直接按【默认执行流程】运行，不发起选项；缺失项 → 只发起缺失项的选项
- 选项结果作为参数传给对应模块调度器（模块调度器不再重复询问已提供的参数）

## 默认执行流程（必须严格按照顺序运行各模块调度器）

说明：模块 = 模块文件夹下的文件

1. 运行 `文本模块/调度器.md` → 文本分析、资产分析
2. 运行 `图片提示词模块/调度器.md`（输出格式按全选项表的选择，默认 文本文件）
3. 发起选项：「资产图是否补全」→ 是 / 否
   - 否 → 终止运行
4. 运行 `视频模块/调度器.md`
